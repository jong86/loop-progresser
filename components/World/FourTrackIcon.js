import React from 'react';
import { View } from 'react-native';

import styles from './_styles_World';

export default class FourTrackIcon extends React.Component {
  render() {
    return (
      <View
        style={styles.fourTrackIcon}>
      </View>
    );
  }
}
