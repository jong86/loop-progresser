import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  main: {
    borderBottomWidth: 2,
    backgroundColor: '#333',
    width: '100%',
    height: '22%',
    justifyContent: 'center',
    alignItems: 'center',
  },

  text: {
    marginBottom: 25,
    color: '#fff',
  },
});