import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  main: {
    backgroundColor: '#226',
    width: 80,
    height: 100,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
